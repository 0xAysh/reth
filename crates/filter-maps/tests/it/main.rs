//! Root module for filter-map integration tests, so the tests are built into a single binary.

mod boundary_padding;
mod genesis_stream;
mod golden;
mod log_values;
mod map_boundaries;
mod resume_anchors;
mod stream_allocations;
mod stream_consumption;

const fn main() {}
